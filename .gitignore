# Terraform-generated directories
.terraform/

# Terraform state files (sensitive data)
*.tfstate
*.tfstate.*

# Terraform variable definition files (potentially sensitive)
*.tfvars
*.tfvars.json

# Crash log files
crash.log

# Local overrides
override.tf
override.tf.json
*_override.tf
*_override.tf.json

# .terraform.lock.hcl can be committed to ensure consistent provider versions
# However, if you prefer to always use the latest, you can ignore it:
# .terraform.lock.hcl